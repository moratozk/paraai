{
  "name": "paraai-firebase-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --project demo-paraai --config ../../firebase.test.json \"node --test firestore.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "@firebase/util": "1.15.1",
    "firebase": "12.14.0",
    "firebase-tools": "15.10.1"
  }
}
